import ExternalAppPage from "./ExternalAppPage";

export default function SVGGenerator() {
  return (
    <ExternalAppPage
      title="SVG Generator"
      description="Open the SVG generation service configured by an administrator."
      urlKey="svg_generator_url"
    />
  );
}